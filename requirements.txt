# Web Application Dependencies
# For FastAPI server, CV parsing, and job recommendations
#
# Expected installation size: ~1.2GB
# - PyTorch: ~700MB (required by sentence-transformers)
# - sentence-transformers models: ~80MB (all-MiniLM-L6-v2)
# - Other dependencies: ~400MB

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# HTML Rendering
lxml==5.3.0

# Data Processing
pandas==2.2.2
pyarrow==14.0.1
numpy>=1.26.0,<2.0.0

# Document Parsing (CV/Resume)
PyPDF2==3.0.1
python-docx==1.1.2

# HTTP Requests
requests==2.31.0

# Environment Variables & Configuration
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# ML for Recommendations & Search
# Note: sentence-transformers automatically installs PyTorch (~700MB)
# Default model: all-MiniLM-L6-v2 (80MB, 384 dims, fast)
# Runtime memory: ~200-300MB with default model
sentence-transformers>=2.2.2
scikit-learn==1.4.0
joblib==1.3.2

# Visualization (CV radar charts)
matplotlib==3.8.2

# System Monitoring (optional)
psutil==5.9.6
