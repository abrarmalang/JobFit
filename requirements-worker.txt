# Worker-specific dependencies
# For data collection, processing, embeddings, and model training

# Data Processing
pandas==2.2.2
pyarrow==14.0.1
numpy>=1.26.0,<2.0.0

# HTTP Requests (for data collection)
requests==2.31.0

# Machine Learning & Embeddings
sentence-transformers>=2.2.2
scikit-learn>=1.3.2
joblib==1.3.2

# PyTorch (CPU-only for workers)
# Note: Install with: pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
torch==2.2.2

# Environment Variables & Configuration
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
